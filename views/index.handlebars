<div class="jumbotron text-center">
  <h1>Burger of The Day!</h1>
  <h4>Help Bob pick a new burger of the day!</h4>
  <p>Help Bob make some new burgers of the day for this week please input your selections below! The Belchers thank you. </p>
</div>

<div class="row">
  <header class="col-12">
    <h3>Suggested Burgers of The Day</h3>
  </header>
  <ul class="col-6" id="burgers-of-the-day">
    {{#each burgers}}
    <li class="{{className}}">
      <h5>{{id}}: {{name}}</h5>
      <button class="btn btn-primary devour" data-burger-id="{{id}}">Devour</button>
    </li>
    {{/each}}
  </ul>
  <ul class="col-6" id="devoured-burgers">
    {{#each devoured}}
    <li class="{{className}}">
      <h5>{{id}}: {{name}}</h5>
      <button class="btn btn-primary devour" data-burger-id="{{id}}">Devour</button>
    </li>
    {{/each}}
  </ul>
</div>

<div class="row">
  <header class="col-12">
    {{!-- TODO put some kind of bobs burger image header --}}
    <h3>Burger of The Day</h3>
  </header>
  <form id="burgerInput" class="col-12">
    <textarea name="burger" id="burger" cols="30" rows="10" placeholder="Say it don't spray it!"></textarea>
    <input type="submit" value="submit">
    <label for="burger">Enter a burger you would want to eat. Then send it to the kitchen!</label>
  </form>
</div>

<footer class="row">
  <div class="col-12">
    <p class="small">No copyright infringment intended. &copy; Whoever.</p>
  </div>
</footer>